* understand the performance approach of select .


(1) the set of current fds should be outside of all the servers and clients
 - Clear the readFd
 - Add the writeFd
 - and the second loop handle the response

as a first approach we will handle only one  server with multiple clients.

Invest at what you know with select (if you need more info go to the linux interface) 
and ask about how why we should do that (1)